#include <cstdio>

int main()
{
    int ;
    while(1)
    {
        for(i = 0; i<6; ++i)
        {
            scanf("%d", &ar[i]);
        }
        for(i = 0; i<6; ++i)
        {


        }

        if(k !=  4 && e!=1)
            printf("Alien\n");
        else if( k == 4 && e==1)
        {
                printf("Bear\n");
        }
        else if( k ==2 && q == 1)
            printf("Elephant\n");
    }
    return 0;
}
