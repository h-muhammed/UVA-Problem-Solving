#include <cstdio>
#include <string.h>

int main()
{
    char ar[50], ar1[50], ar2[50];
    int i, j, len1, len2,t;
    scanf("%d", &t);
    for(i =1; i<=t; ++i)
    {
        scanf("%s %s", &ar, &ar1);
        len1 = ar;
        len2 = ar2;
        if(len2>len1)
        d=len2;
        else
        d=len1;
        for(j = 0; j< len; ++j)
        {

        }
    }
}
